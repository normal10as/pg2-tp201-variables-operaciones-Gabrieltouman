Module Ecuacion
    Sub Main()
        Dim a As SByte = 1
        Dim b As SByte = 5
        Dim c As SByte = 2
        Console.WriteLine("X = " & (b ^ 2 - 4 * a * c) / 2 * a)
        Console.ReadKey()
    End Sub

End Module
