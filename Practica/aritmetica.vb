Module aritmetica
    Sub Main()
        Dim a As SByte = -3
        Dim b As SByte = 2
        Dim c As Single = -2.5
        Dim d As Single = 6.3
        Console.WriteLine(a + b)
        Console.WriteLine(a + c)
        Console.WriteLine(a - b)
        Console.WriteLine(a - d)
        Console.WriteLine(a * b)
        Console.WriteLine(a * c)
        Console.WriteLine(a / d)
        Console.WriteLine(a / c)
        Console.ReadKey()
    End Sub

End Module
